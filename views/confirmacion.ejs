<!DOCTYPE html>
<html>
<head>
    <title>Confirmación de Matrícula</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
</head>
<body>
    <div class="container mt-5">
        <h1 class="text-center">Confirmación de Matrícula</h1>
        <hr>

        <h3>Curso seleccionado:</h3>
        <p><strong>Curso:</strong> <%= curso.name %></p>

        <h3>Medio de pago seleccionado:</h3>
        <p><strong>Medio de Pago:</strong> <%= medioPago %></p>

        <h3>Modulo seleccionado:</h3>
        <p><strong>Modulo:</strong> <%= Array.isArray(modulos) ? modulos.join(', ') : modulos %></p>

        <h3>Total a pagar:</h3>
        <p><strong>Total a pagar:</strong> $<%= precioTotalConDescuento.toFixed(2) %></p>

        <h3>Datos de confirmación:</h3>
        <p>¡Su matrícula ha sido confirmada! Los siguientes son los detalles de su matrícula:</p>

        <ul>
            <li><strong>Curso:</strong> <%= curso.name %></li>
            <li><strong>Medio de Pago:</strong> <%= medioPago %></li>
            <li><strong>Modulo:</strong> <%= Array.isArray(modulos) ? modulos.join(', ') : modulos %></li>
            <li><strong>Total a pagar:</strong> $<%= precioTotalConDescuento.toFixed(2) %></li>

            
        </ul>
    </div>
    
</body>
</html>
